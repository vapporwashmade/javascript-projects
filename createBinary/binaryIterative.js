// takes in a number and returns an array
// creates binary combinations for that number
function f(n) {
	var binArray = ['0','1'];
	for (var i = 1; i < n; i++) {
		
	}
	return binArray;
}
console.log(f(3));